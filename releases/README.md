# Release notes

Patch notes for each release are generated automatically when you run `scripts\release.bat`.

- Per-release markdown: `releases/vX.Y.Z.md`
- Cumulative log: `CHANGELOG.md` at the repo root
- Version source of truth: `VERSION` (synced to package.json, tauri.conf.json, and Cargo.toml)
